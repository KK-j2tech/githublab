name: main
on: push

jobs:
  job1:
   name: JOB1
   runs-on: ubuntu-latest
   steps:
     - name: step1
       run: |
            pwd
            hostname
            ls -ltr
     - name: step2 - checkout
       uses: actions/checkout@v6.0.2
     - name: step3
       run: |
             pwd
             hostname
             ls -ltr
